# Пример сценария прогона: сверка первой итерации находит расхождение, второй —
# совпадение. Прогон по нему проходит обратное ребро схемы цикла и замыкается
# на второй итерации.
intent: INT-001
positions:
  - поле FR.source_ref
  - связь derives
stages:
  reconcile: [rejected, passed]
