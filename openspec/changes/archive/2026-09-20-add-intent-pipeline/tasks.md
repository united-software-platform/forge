## 1. Контракт шага

- [x] 1.1 Реализовать `tools/forge_model/intent/step.py`: перечень стадий в фиксированном порядке,
  четыре исхода и результат шага (исход, сообщение, вердикты по позициям); проверка — тест
  на построение результата каждого исхода
- [x] 1.2 Реализовать приём набора «стадия → шаг» с проверкой полноты до выполнения первой стадии;
  проверка — тест: неполный набор даёт ошибку процесса и ни один шаг не вызван
- [x] 1.3 Реализовать отказ на неизвестном исходе шага; проверка — тест: шаг, вернувший значение
  вне перечня, завершает прогон ошибкой с указанием стадии

## 2. Состояния и переходы

- [x] 2.1 Реализовать `tools/forge_model/intent/state.py`: закрытый перечень восьми состояний
  и таблица разрешённых переходов по [решению 4](./design.md#решение-4-состояние-ведёт-машину-переходов-а-не-лестницу-фактов);
  проверка — тест сверяет таблицу с диаграммой состояний документа цикла
- [x] 2.2 Реализовать применение перехода с отказом на неразрешённом; проверка — тест: запрещённый
  переход даёт ошибку процесса с указанием исходного и запрошенного состояния
- [x] 2.3 Проверить полный успешный проход состояний Черновик → … → Подтверждено; проверка — тест
  сверяет последовательность целиком, а не конечное состояние

## 3. Сценарий прогона и подставные шаги

- [x] 3.1 Реализовать `tools/forge_model/intent/scenario.py`: чтение сценария — код намерения,
  позиции, исходы стадий по итерациям; ошибки формата поднимают `ModelError`; проверка — тесты
  на корректный сценарий и на каждую ошибку формата
- [x] 3.2 Реализовать правила умолчаний: не названная стадия считается пройденной, короткий список
  повторяет последнее значение; проверка — тесты на оба правила
- [x] 3.3 Реализовать `tools/forge_model/intent/mock.py` — подставные шаги на все шесть стадий,
  берущие исход из сценария по номеру итерации; проверка — тест: шаг не читает файлов модели
  и не открывает подключений
- [x] 3.4 Положить пример сценария `model/intents/scenarios/demo.yaml`, описывающий расхождение
  на первой итерации и совпадение на второй; проверка — прогон по нему замыкает цикл

## 4. Движок цикла

- [x] 4.1 Реализовать `tools/forge_model/intent/engine.py`: последовательное выполнение стадий
  с остановом на первом исходе, отличном от «пройдено»; проверка — тест: последующие стадии
  не вызваны
- [x] 4.2 Реализовать уход в доработку и открытие следующей итерации, пока лимит не исчерпан;
  проверка — тест на сценарии «отклонено, затем пройдено»: две итерации, состояние «Подтверждено»
- [x] 4.3 Реализовать прерывание по исчерпании лимита с переводом в «Прервано» и фиксацией
  последнего результата; проверка — тест: число итераций равно лимиту, намерение не подтверждено
- [x] 4.4 Реализовать правила зачёта итерации: исход «остановлено» и повторный прогон
  подтверждённого намерения номер не увеличивают; проверка — тесты на оба случая
- [x] 4.5 Реализовать лимит по умолчанию 3 с переопределением параметром; проверка — тест
  на умолчание и на переопределённое значение

## 5. Отчёт прогона

- [x] 5.1 Реализовать `tools/forge_model/intent/report.py`: машиночитаемый отчёт с кодом намерения,
  номером итерации, конечным состоянием, исходами стадий, вердиктами и причиной завершения;
  проверка — тест сверяет состав полей
- [x] 5.2 Реализовать запись отчёта при любом исходе, включая ошибку процесса, без затирания
  предыдущих итераций; проверка — тесты на запись при ошибке и на сохранность прежних отчётов
- [x] 5.3 Реализовать вывод номера следующей итерации из лежащих отчётов; проверка — тест:
  прогон на непустом каталоге продолжает нумерацию
- [x] 5.4 Исключить каталог `model/intents/runs/` из индекса Git; проверка — `git status` после
  прогона чист

## 6. Точки входа

- [x] 6.1 Добавить подкоманду `intent run` в `tools/forge_model/cli.py` с параметрами сценария,
  каталога отчётов и лимита; проверка — тест на разбор аргументов и на значения по умолчанию
- [x] 6.2 Реализовать коды возврата `0`/`1`/`2` по
  [решению 6](./design.md#решение-6-раскладка-отчёты-коды-возврата); проверка — тесты на код
  каждого исхода: замкнутый цикл, ошибка процесса, останов, прерывание по лимиту
- [x] 6.3 Добавить цель `make intent-run SCENARIO=... [LIMIT=...]`; проверка —
  `sh tools/make-lint/check.sh` проходит по норме 30 строк

## 7. Проверка каркаса целиком

- [x] 7.1 Написать тест на подмену шага: предметная реализация одной стадии встаёт в набор вместо
  подставной, движок не изменён; проверка — прогон проходит, поведение остальных стадий прежнее
- [x] 7.2 Написать прогон по набору сценариев, покрывающий все ветви: успешный проход, отклонение
  валидации, останов, расхождение с последующим совпадением, исчерпание лимита; проверка —
  ожидаемые состояние, число итераций и код возврата у каждой ветви
- [x] 7.3 Проверить воспроизводимость и независимость от окружения: два прогона одного сценария
  на пустом каталоге отчётов дают одинаковый результат, хранилище не поднято; проверка — тест
  проходит без профиля `db`
- [x] 7.4 Прогнать `make check` и `make model-test`; проверка — оба завершаются без замечаний
- [x] 7.5 Дополнить раздел ограничений `docs/model-intent-loop.md`: оркестрация цикла реализована,
  стадии остаются подставными, предметные реализации въезжают отдельными изменениями; запись
  в таблице версий по [DOC-022](../../../rules/documentation-rules.md#жизненный-цикл-документа)
- [x] 7.6 Прогнать `openspec validate add-intent-pipeline --strict`; проверка — валидация проходит

## Версионирование

| Версия | Дата | Задача | Агент | Модель | Описание изменений |
|--------|------|--------|-------|--------|--------------------|
| 1.0.0 | 2026-09-20 | OpenSpec change `add-intent-pipeline` | Claude Code | Claude Opus 5 | Начальное создание: шесть групп задач — артефакт намерения, валидация стадий 1–2, сопоставление позиции с фактом, состояние с отчётом и лимитом, оркестратор с точками входа, сквозная проверка и документация |
| 1.1.0 | 2026-09-20 | Уточнение пользователя: нужен работоспособный каркас цикла с подставными шагами | Claude Code | Claude Opus 5 | Кардинальная переработка: задачи пересобраны под движок — контракт шага, состояния и переходы, сценарий с подставными шагами, движок с итерациями и лимитом, отчёт, точки входа, проверка каркаса подменой шага и набором сценариев. Удалены группы задач по валидации заявки, сопоставлению позиций с фактом и сквозному прогону на временном хранилище: предметных стадий в заготовке нет, хранилище тестам не требуется |
